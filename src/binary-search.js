/**
 * Busca el elemento en un array ussando la función de comparación
 * @param {Array.<number>} elements array con los elementos sobre los que queremos buscar
 * @returns {number}
 * @example
 * binarySearch([1, 2, 3, 4, 5, 6], 2) // => 2
 * binarySearch([1, 2, 3, 4, 5, 6], 10) // => null
 */
function binarySearch(numbers, comparator) {
  return // ???
}

module.exports = {
  binarySearch
}
